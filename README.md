# Playwright API Automation (TypeScript)

This project demonstrates how to do API test automation using **Playwright** with **TypeScript** against the public REST API **https://reqres.in**.

## Tech Stack
- Node.js
- Playwright (`@playwright/test`)
- TypeScript

## Setup

```bash
npm install
npx playwright install
